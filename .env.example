# Database Configuration
# MariaDB/MySQL Connection Details
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=wikimillionaire

# Alternative: Connection String (if provided, individual settings above are ignored)
# DATABASE_URL=mysql://user:password@host:port/database

# Wikimedia OAuth Configuration
# The app supports both confidential and public (non-confidential) OAuth clients:
#
# Confidential client (recommended for server-side apps):
#   - Set both WIKIMEDIA_CLIENT_ID and WIKIMEDIA_CLIENT_SECRET
#   - Uses client_secret for authentication (no PKCE)
#
# Public/Non-confidential client (for client-side apps):
#   - Set only WIKIMEDIA_CLIENT_ID (leave WIKIMEDIA_CLIENT_SECRET empty or unset)
#   - Uses PKCE (Proof Key for Code Exchange) for authentication
#
WIKIMEDIA_CLIENT_ID=your_client_id
WIKIMEDIA_CLIENT_SECRET=your_client_secret
WIKIMEDIA_REDIRECT_URI=http://localhost:3000/api/auth/callback/wikimedia

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_key_here

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
